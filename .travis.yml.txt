language: python
python:
  - 3.6
install:
  - pip install sphinx sphinx_rtd_theme

script:
  - make -C docs/ html
  - touch docs/build/html/.nojekyll

after_success:

deploy:
  - provider: pages:git
    verbose: true
    token: $GITHUB_TOKEN
    edge: true
    local_dir: ./docs/build/html/
    keep_history: true
